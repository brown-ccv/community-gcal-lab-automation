# Authentication Configuration# Environment Variables Template

# Get these from Google Cloud Console (Web Application OAuth client)# Copy this to .env for local development (not needed for production)

AUTH_CLIENT_ID=your-web-auth-client-id-here

AUTH_CLIENT_SECRET=your-web-auth-client-secret-here# Port (default: 3000)

PORT=3000

# Session secret - use a random, secure string

# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"# OAuth redirect URI

SESSION_SECRET=your-random-session-secret-here# Local development: http://localhost:3000/oauth2callback

# Production: https://your-app-name.onrender.com/oauth2callback

# Allowed email domain (case-sensitive, no @ symbol)REDIRECT_URI=http://localhost:3000/oauth2callback

ALLOWED_DOMAIN=brown.edu

# Node environment

# OAuth callback URLNODE_ENV=development

# Local: http://localhost:3000/auth/google/callback
# Production: https://your-app.onrender.com/auth/google/callback
AUTH_CALLBACK_URL=http://localhost:3000/auth/google/callback

# Development Mode
# Set to 'true' to bypass authentication (DEVELOPMENT ONLY!)
# NEVER set this to 'true' in production
BYPASS_AUTH=false

# Demo Mode (separate from authentication)
# Set to 'true' to simulate calendar events without making real API calls
DEMO_MODE=false

# Server Configuration
PORT=3000
NODE_ENV=development
